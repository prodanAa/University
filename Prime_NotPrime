#include <iostream>
using namespace std;
int main()
{
	int n, prime = 1;
	cin >> n;
	for (int d = 2; d <= n / 2; d++)
		if (n % d == 0)
		{
			prime = 0; break;
		}
	if (prime) cout << "Prime" << endl;
	else cout << "Not Prime" << endl;
	system("pause");
	return 0;
}
